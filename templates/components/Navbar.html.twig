<div{{ attributes }} >

    <div class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <div class="container d-inline-flex align-items-center">
            <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
                <!-- Logo -->
                <img src="{{ asset('uploads/images/logo.png') }}" alt="Logo" width="80" height="80">
            </a>

            <ul class="nav nav-pills">
                <li class="nav-item"><a href="{{ path('app_home') }}" class="nav-link">Accueil</a></li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item"><a href="{{ path('app_event_index') }}" class="nav-link">Événement</a></li>
                    <li class="nav-item"><a href="{{ path('app_user_profile') }}" class="nav-link">Profil</a></li>
                    <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link">Se déconnecter</a></li>
                {% else %}
                    <li class="nav-item"><a href="{{ path('app_login') }}" class="nav-link">Se connecter</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</div>