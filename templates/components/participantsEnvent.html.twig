  
    <h2 class="mb-2">Liste des participants inscrits :</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Pseudo</th>
                <th>Nom</th>
            </tr>
        </thead> 
        <tbody>
         {% if participants %}
            {% for participant in participants %}
            <tr>
                <td>{{ participant.username }}</td>
                <td>{{ participant.firstname }} {{ participant.name }}</td>
            </tr>
            {% endfor %}
        {% else %}
            <tr> <b>Aucun participant enregistré ! </b></tr>
        {% endif %}
        </tbody>
    </table>