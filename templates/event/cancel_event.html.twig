{% extends 'base.html.twig' %}

{% block title %}Event index{% endblock %}

{% block body %}
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <h1>Annuler l'événement</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Enregistrer</button>
        <a href="{{ path('app_event_index') }}">Annuler</a>
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
