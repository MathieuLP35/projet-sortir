{% extends 'base.html.twig' %}

{% block title %}Modifier son mot de passe{% endblock %}

{% block body %}
    <h1>Modifier son mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Modifier son mot de passe</button>
    {{ form_end(resetForm) }}
{% endblock %}
